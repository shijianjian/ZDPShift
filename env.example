# ZDPShift — optional overrides. The three backbones are git submodules under
# third_party/ and resolve automatically; set these only if you relocate them.
# export FOUNDATIONSTEREO_ROOT="$PWD/third_party/FoundationStereo"
# export IGEV_ROOT="$PWD/third_party/IGEV/IGEV-Stereo"
